<h1>ジャンル編集</h1>
<%= form_with model: @genre, url: genre_update_path(@genre.id), method: :patch do |f| %>
  <div class="field">
       <%= f.label :商品名 %>
       <%= f.text_field :name %>
       <%= f.submit "変更を保存" %>
  </div>
<% end %>