<h1>商品一覧ページ</h1>
<%= link_to "＋", admin_items_new_path %></br>
<% @items.each do |item| %>
    <tr>
      <td><%= item.id %></td>
      <td><%= item.name %></td>
      <td><%= item.price %></td>
      <td><%= item.genre_id %></td>
      <td><%= item.is_active %></td>
    </tr>
  <% end %>